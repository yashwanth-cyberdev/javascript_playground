/* Variable is used to store some information.
So, for example it is like a container that can store some value in it.
We can store some data using assignment operator.
*/

// declaration:

let age;
age = 20; // Initialization in a new line

// initialization:

let username = "cyber-dev";

// Multiple declaration:

let firstname = "John",
  lastname = "Doe";

// We can create a variable using keywords var, let and const
